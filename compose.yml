services:
  db:
    image: anilkumarnaraplli/pyapp:db-image
    deploy:
      replicas: 1
    ports:
      - "3306:3306"
    networks:
      - mynetwork

  auth_service:
    image: anilkumarnaraplli/pyapp:auth-image
    deploy:
      replicas: 1
    networks:
      - mynetwork

  book_service:
    image: anilkumarnaraplli/pyapp:book-image
    deploy:
      replicas: 1
    depends_on:
      - db
    networks:
      - mynetwork

  borrow_service:
    image: anilkumarnaraplli/pyapp:borrow-image
    deploy:
      replicas: 1
    networks:
      - mynetwork

  frontend:
    image: anilkumarnaraplli/pyapp:frontend
    deploy:
      replicas: 1
    ports:
      - "5000:5000"
    depends_on:
      - auth_service
      - book_service
      - borrow_service
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: overlay
